[package]
name = "splitpaint"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.24"
egui = "0.24"
image = "0.24"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
rfd = "0.14"

[target.'cfg(windows)'.dependencies]
winapi = { version = "0.3", features = ["winuser"] }